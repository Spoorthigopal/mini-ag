import json
import os

skills_metadata = [
    {"id": "javascript", "name": "JavaScript"},
    {"id": "python", "name": "Python"},
    {"id": "react", "name": "React"},
    {"id": "system-design", "name": "System Design"},
    {"id": "dsa", "name": "Data Structures & Algorithms"}
]

# Comprehensive ~400-500 word deep dives for each topic to guarantee the total answer is 500-1000 words
DEEP_DIVES = {
    "javascript": """

---
**Core JavaScript Deep Dive**
JavaScript, originally built in 10 days by Brendan Eich, has evolved from a simple client-side scripting language to a ubiquitous, multi-paradigm programming language that dominates both front-end and back-end development. At its core, JavaScript is a dynamic, weakly typed language that utilizes just-in-time (JIT) compilation. Unlike strongly typed languages like Java or C++, JavaScript does not enforce strict type declarations, allowing variables to be reassigned to values of different types dynamically. While this provides immense flexibility, it also necessitates rigorous testing and often drives developers toward TypeScript for better type safety.

One of the most defining characteristics of JavaScript is its execution model based on an Event Loop. Since JavaScript operates on a single thread—meaning it can only execute one piece of code at a time—it heavily relies on an asynchronous, non-blocking I/O model to remain responsive. When executing code, synchronous tasks are pushed onto the Call Stack. If an asynchronous operation (like an HTTP request or a setTimeout callback) is encountered, it is offloaded to Web APIs provided by the browser (or the C++ thread pool in Node.js). Once completed, the callback is moved to the Task Queue (or Microtask Queue for Promises). The Event Loop constantly monitors the Call Stack, and whenever it is empty, it pushes the first item from the queue onto the stack for execution. This architecture is what allows Node.js to handle thousands of concurrent connections efficiently despite being single-threaded.

Another foundational concept in JavaScript is its Prototype-Based Inheritance. Rather than relying on rigid, class-based inheritance like classical object-oriented languages, JavaScript objects inherit directly from other objects via the prototype chain. Every object has an internal `[[Prototype]]` property pointing to another object, eventually terminating at `null`. When attempting to access a property or method on an object, the engine first checks the object itself, and if it fails to find it, traverses up the prototype chain. Even though ES6 introduced the `class` keyword, this is largely syntactical sugar over this prototypal architecture.

Variable scoping has also historically been a crucial topic. Before ES6, variables were declared exclusively with `var`, which is function-scoped and hoisted to the top of its execution context. This often led to confusing bugs where variables bled outside of blocks like loops or `if` statements. The introduction of `let` and `const` solved this by providing block scope and preventing access before initialization (the Temporal Dead Zone). Together with closures—functions that "remember" their lexical environment even when executed outside of it—these scoping rules enable advanced functional programming patterns like currying, partial application, and the module pattern, making modern JavaScript an incredibly powerful tool for engineering complex applications.
""",
    "python": """

---
**Core Python Deep Dive**
Python, created by Guido van Rossum and released in 1991, has become one of the most prominent programming languages worldwide, renowned for its readability, versatility, and "batteries-included" standard library. It is a high-level, interpreted language with dynamic semantics. Its design philosophy emphasizes code readability, heavily relying on significant indentation to define code blocks instead of curly braces or keywords. This design choice inherently forces developers to write cleaner code, making it an excellent language for both beginners and complex enterprise systems.

Python is a multi-paradigm language, supporting object-oriented, imperative, and, to a lesser extent, functional programming styles. In Python, everything is an object, from simple integers and strings to functions and modules. Its memory management is handled automatically by a private heap space and a garbage collector that uses a combination of reference counting and a generational garbage collection algorithm to manage cyclical references. This abstractions significantly speeds up development cycles by freeing the programmer from manual memory allocation tasks.

A fundamental aspect of Python's execution model is the Global Interpreter Lock, commonly referred to as the GIL. The GIL is a mutex (lock) in CPython—the standard and most widely used implementation of Python—that protects access to Python objects, preventing multiple native threads from executing Python bytecodes simultaneously. The GIL exists because CPython's memory management is not thread-safe. While the GIL dramatically simplifies the implementation of CPython and makes single-threaded programs extremely fast, it creates a bottleneck for multi-threaded, CPU-bound applications. Consequently, multi-threading in Python is primarily useful for I/O-bound tasks (like web scraping or file operations) where threads spend most of their time waiting. For CPU-bound tasks, developers must leverage the `multiprocessing` module, which creates separate processes, each with its own Python interpreter and memory space, to achieve true parallelism across multiple CPU cores.

Another powerful feature of Python is its support for meta-programming and advanced decorators. Decorators, denoted by the `@` symbol, are higher-order functions that modify or enhance the behavior of other functions or classes without permanently altering their source code. They wrap a function, executing code before or after the target function runs, which is exceptionally useful for cross-cutting concerns like logging, access control, memoization, and timing execution. Furthermore, Python's dynamic typing allows for "duck typing," summarized by the phrase "If it walks like a duck and quacks like a duck, it must be a duck." This means that an object's suitability is determined by the presence of certain methods and properties, rather than its actual type, fostering a highly flexible and polymorphic development environment.
""",
    "react": """

---
**Core React Deep Dive**
React, developed and open-sourced by Facebook, revolutionized front-end development by introducing a component-based architecture and a declarative approach to building user interfaces. Before React, most libraries relied on direct manipulation of the Document Object Model (DOM), which is notoriously slow and often led to hard-to-maintain spaghetti code. React abstracted the DOM away using its most famous innovation: the Virtual DOM. 

The Virtual DOM is a lightweight, in-memory representation of the actual DOM. When the state of a React component changes, React creates a new Virtual DOM tree and compares it against the previous one using a highly optimized heuristic algorithm known as "diffing." This process, called reconciliation, calculates the minimal set of DOM mutations required to sync the real DOM with the Virtual DOM. By batching these updates and applying them simultaneously, React minimizes expensive layout thrashing and repainting in the browser, achieving remarkable rendering performance even in complex, data-heavy applications.

React's programming model is fundamentally built around components, which are reusable, self-contained pieces of UI that manage their own state and accept inputs called "props." With the introduction of React Hooks in version 16.8, functional components gained the ability to manage state and side effects, largely deprecating the older, more verbose class-based components. Hooks like `useState` allow components to hold local state, while `useEffect` handles side effects such as data fetching, subscriptions, and manual DOM manipulation. The rule of Hooks—that they must be called at the top level and only from React functions—ensures that state and lifecycle hooks are consistently mapped across renders, maintaining the predictability of the component's state.

Beyond state management, React heavily emphasizes a unidirectional data flow. Data is passed down from parent components to child components via props. If a child needs to modify state managed by a parent, it must invoke a callback function passed down as a prop. This predictable data flow makes it significantly easier to debug applications and track where state changes originate. For highly complex applications where prop-drilling becomes cumbersome, developers typically turn to state management solutions like the Context API, Redux, or Zustand. Furthermore, React's ecosystem is vast, offering solutions for server-side rendering (like Next.js) which improve SEO and initial load times, and React Native, which allows developers to use React paradigms to build native mobile applications for iOS and Android. This write-once, deploy-anywhere philosophy solidifies React as a foundational pillar of modern web development.
""",
    "system-design": """

---
**System Design Deep Dive**
System design is the process of defining the architecture, components, modules, interfaces, and data for a system to satisfy specific requirements. It is the critical transition phase between understanding what a system must do and actually coding it. In the context of modern software engineering, system design almost always refers to distributed systems—architectures that consist of multiple independent computers that communicate and coordinate their actions by passing messages over a network. Designing robust distributed systems requires carefully balancing trade-offs regarding scalability, reliability, availability, and performance.

A foundational principle in system design is the CAP theorem, which states that a distributed data store can only simultaneously guarantee two of three properties: Consistency (every read receives the most recent write or an error), Availability (every request receives a non-error response, without the guarantee that it contains the most recent write), and Partition tolerance (the system continues to operate despite an arbitrary number of messages being dropped or delayed by the network). Because network partitions (P) are an unavoidable reality of distributed systems, architects must choose between Consistency (CP systems, like MongoDB or HBase) and Availability (AP systems, like Cassandra or DynamoDB) during network failures.

Scalability is typically achieved through two main strategies: vertical scaling and horizontal scaling. Vertical scaling (scaling up) involves adding more resources—CPU, RAM, or disk space—to an existing server. While simpler, vertical scaling has hard physical limits and introduces a single point of failure. Horizontal scaling (scaling out) involves adding more servers to a resource pool. This approach requires load balancers to distribute incoming network traffic across multiple servers. Load balancers act as reverse proxies and can use various algorithms (Round Robin, Least Connections, IP Hashing) to route traffic, drastically improving the responsiveness and availability of applications.

Data management is another extremely complex aspect of system design. As databases grow, they must be partitioned or "sharded" across multiple machines. Database sharding involves splitting a large database into smaller, faster, more easily managed parts called shards. Sharding logic can be complex and requires a reliable shard key to ensure even distribution of data. Furthermore, to reduce latency and database load, engineers aggressively use caching layers (like Redis or Memcached). Caches temporarily store frequently accessed data in memory, providing lightning-fast read access. Finally, asynchronous processing using message queues (like Kafka or RabbitMQ) is used to decouple system components, allowing heavy tasks like video processing or sending emails to be executed in the background, ensuring the main application remains highly responsive to user requests.
""",
    "dsa": """

---
**Data Structures & Algorithms Deep Dive**
Data Structures and Algorithms (DSA) form the absolute bedrock of computer science and software engineering. At a fundamental level, a data structure is a specialized format for organizing, processing, retrieving, and storing data, while an algorithm is a step-by-step procedure or formula for solving a problem. The choice of the right data structure combined with the right algorithm can be the difference between a program that executes in milliseconds and one that takes hours, making DSA critical for performance optimization and technical interviews.

The performance of algorithms is universally analyzed using Asymptotic Notation, most commonly Big O Notation. Big O describes the worst-case scenario or upper bound of an algorithm's time complexity (how runtime grows as the input size grows) and space complexity (how memory usage grows). For example, accessing an element in an array by its index is an O(1) constant time operation, while searching an unsorted array is an O(N) linear time operation. Sorting algorithms like QuickSort and MergeSort operate at O(N log N) time complexity, which is significantly faster than O(N^2) algorithms like Bubble Sort or Insertion Sort for large datasets. Understanding these complexities enables developers to predict system behavior under heavy load and scale applications efficiently.

Key data structures include Arrays, Linked Lists, Stacks, Queues, Hash Tables, Trees, and Graphs. Hash Tables, for instance, map keys to values for highly efficient lookups, typically achieving O(1) average time complexity. They achieve this using a hash function to compute an index into an array of buckets. When collisions occur (two keys hashing to the same index), they are resolved via techniques like chaining (using linked lists at the bucket) or open addressing (probing for the next empty slot). Trees, specifically Binary Search Trees (BST), allow for efficient hierarchical data storage, offering O(log N) lookup times if balanced (like AVL or Red-Black trees). Graphs, which consist of nodes (vertices) connected by edges, are essential for representing networks, social connections, and routing paths, and are traversed using algorithms like Breadth-First Search (BFS) and Depth-First Search (DFS).

On the algorithmic side, advanced techniques like Dynamic Programming (DP) and Greedy Algorithms are critical for solving complex optimization problems. Dynamic Programming solves complex problems by breaking them down into simpler, overlapping subproblems and storing the results of these subproblems (memoization or tabulation) to avoid redundant computations. This transforms exponential time complexities O(2^N) into polynomial ones. Greedy algorithms, on the other hand, make the locally optimal choice at each stage with the hope of finding a global optimum. While greedy algorithms are faster and simpler, they do not guarantee an optimal solution for all problems, unlike DP. Mastery of DSA allows software engineers to write highly optimized, scalable, and efficient code, which is why it remains the gold standard for evaluating technical competence in the industry.
"""
}

# The questions database: 10 per difficulty per skill
questions_data = {
    "javascript": {
        "easy": [
            ("What is the difference between var, let, and const?", "Variables declared with var are function-scoped and hoisted to the top of their scope, meaning they can be accessed before they are initialized (resulting in undefined). They also allow redeclaration. In contrast, let and const are block-scoped and are hoisted but reside in the Temporal Dead Zone (TDZ) until their definition is evaluated, preventing access before initialization. The key difference between let and const is that let allows variable reassignment, whereas const enforces an immutable binding, meaning the identifier cannot be reassigned to a different value (though the contents of an object or array declared with const can still be mutated)."),
            ("What are JavaScript data types?", "JavaScript defines seven primitive data types: String, Number, Boolean, Null, Undefined, Symbol (introduced in ES6 for unique identifiers), and BigInt (for extremely large integers). Primitive values are immutable and compared by value. Additionally, there is one non-primitive structural type: Object. The Object type includes plain objects, Arrays, Functions, Dates, and Regular Expressions. Objects are mutable and compared by reference, meaning two distinct object instances with identical properties are strictly not equal."),
            ("What is the difference between == and ===?", "The double equals (==) is the loose equality operator. It compares two values for equality but performs type coercion if the types differ, meaning it converts them to a common type before comparison (e.g., '5' == 5 evaluates to true). The triple equals (===) is the strict equality operator. It compares both the value and the type without performing any type coercion. If the types are different, it immediately returns false (e.g., '5' === 5 evaluates to false). Best practices mandate using strict equality to avoid unexpected bugs stemming from complex coercion rules."),
            ("How do you check if an object is an array?", "Before ES5, developers relied on checking the object's constructor or using `Object.prototype.toString.call(obj) === '[object Array]'`. However, modern JavaScript provides a built-in static method: `Array.isArray(obj)`. This is the most reliable and idiomatic way to determine if a value is an Array, as it works accurately even across different execution contexts like iframes, where the `instanceof Array` operator might fail due to differing prototype chains."),
            ("What is hoisting?", "Hoisting is JavaScript's default behavior of moving variable and function declarations to the top of their respective containing scopes (either global or function) during the compile phase, before the code is executed. It is crucial to note that only the declarations are hoisted, not the initializations. While `var` declarations are hoisted and initialized with `undefined`, `let` and `const` declarations are hoisted but remain uninitialized in the Temporal Dead Zone. Function declarations are completely hoisted, meaning you can call a function before it appears in the code, whereas function expressions assigned to variables are not fully hoisted."),
            ("What is `NaN`?", "`NaN` stands for 'Not-a-Number' and is a special global property representing a computational error or an unrepresentable value resulting from an invalid mathematical operation (e.g., dividing zero by zero, or multiplying a string by a number). Interestingly, the `typeof NaN` evaluates to `'number'`. A unique characteristic of `NaN` is that it is not equal to anything, including itself (`NaN === NaN` is false). To reliably check if a value is `NaN`, you should use the built-in `Number.isNaN()` method, which does not forcefully coerce the argument to a number like the older global `isNaN()` function."),
            ("Explain the concept of scope.", "Scope determines the accessibility and visibility of variables, functions, and objects in specific parts of your code during runtime. JavaScript primarily uses lexical scoping, meaning scope is determined by the physical placement of variables and blocks in the source code. The three main types are Global Scope (accessible from anywhere), Function/Local Scope (accessible only within the function it was declared), and Block Scope (introduced with ES6 `let` and `const`, restricting variables to the `{ }` block they are defined in). Proper scope management prevents variable collisions and unintended side effects."),
            ("What are template literals?", "Template literals, introduced in ES6, are string literals that allow for embedded expressions and multi-line strings. They are enclosed by backticks (`) instead of single or double quotes. Template literals support string interpolation, letting you inject variables and expressions directly into the string using the `${expression}` syntax. This drastically improves readability and eliminates the need for cumbersome string concatenation using the `+` operator. They also support tagged templates, which allow a function to parse template literals dynamically."),
            ("What is the DOM?", "The Document Object Model (DOM) is a programming interface for HTML and XML documents. It represents the page as a logical tree, where each branch ends in a node, and each node contains objects representing parts of the document. The DOM provides a structural representation of the document, allowing programming languages like JavaScript to access, modify, and manipulate the document structure, style, and content dynamically. Web APIs provide methods to query nodes (like `document.getElementById` or `document.querySelector`) and attach event listeners to them."),
            ("Explain the `this` keyword.", "In JavaScript, the `this` keyword refers to the object it belongs to, but its value depends heavily on the invocation context. In the global scope, `this` refers to the global object (e.g., `window` in browsers). Inside a regular function, `this` refers to the global object, but in strict mode, it is `undefined`. When a function is called as a method of an object, `this` points to that object. In a constructor function or class, `this` refers to the newly created instance. Importantly, ES6 arrow functions do not have their own `this` binding; instead, they lexically inherit `this` from their surrounding scope.")
        ],
        "moderate": [
            ("What is a closure?", "A closure is the combination of a function bundled together (enclosed) with references to its surrounding state (the lexical environment). In JavaScript, closures are created every time a function is created, at function creation time. This mechanism allows the inner function to access variables from its outer (enclosing) function's scope even after the outer function has returned. Closures are heavily utilized in JavaScript for data privacy (creating private variables and methods), currying, and maintaining state in event handlers and callbacks."),
            ("Explain the Event Loop.", "The Event Loop is the mechanism that allows JavaScript to perform non-blocking I/O operations despite being single-threaded. It continuously monitors two main areas: the Call Stack and the Callback/Task Queues. Synchronous code executes in the Call Stack. When an asynchronous operation finishes (like an API call), its callback is pushed to a queue. If the Call Stack is completely empty, the Event Loop takes the first task from the queue and pushes it onto the Call Stack to execute. Microtasks (like resolved Promises) have a higher priority and are processed before Macrotasks (like setTimeout)."),
            ("What are Promises?", "A Promise is an object representing the eventual completion or failure of an asynchronous operation and its resulting value. It acts as a proxy for a value not necessarily known when the promise is created. A Promise can be in one of three states: pending (initial state), fulfilled (operation completed successfully), or rejected (operation failed). Promises solve the problem of 'callback hell' by allowing asynchronous operations to be chained linearly using `.then()` and `.catch()`, improving code readability and error handling logic."),
            ("Explain Prototypal Inheritance.", "Unlike classical inheritance that uses rigid classes, JavaScript utilizes prototypal inheritance where objects inherit directly from other objects. Every JavaScript object has an internal link to another object called its prototype. When trying to access a property that does not exist on an object, JavaScript engine searches up the prototype chain until it finds the property or reaches `null`. This allows for memory-efficient sharing of methods across instances. The ES6 `class` syntax is primarily syntactic sugar over this core prototypal model."),
            ("What is the difference between `call`, `apply`, and `bind`?", "These are methods used to explicitly set the `this` context for a function. `call()` invokes the function immediately with a specified `this` context and arguments passed individually (comma-separated). `apply()` is nearly identical to `call()`, but arguments are passed as a single array. `bind()` does not invoke the function immediately; instead, it returns a new function with the `this` context permanently bound to the provided value, which can be executed later. `bind` is particularly useful for preserving context in event listeners and asynchronous callbacks."),
            ("How does async/await work?", "`async/await` is ES2017 syntactic sugar built on top of Promises, designed to make asynchronous code look and behave more like synchronous code. Applying the `async` keyword to a function ensures it automatically returns a Promise. The `await` keyword can only be used inside an `async` function and pauses the execution of that specific function until the awaited Promise resolves or rejects. This eliminates `.then()` chains and significantly simplifies error handling when combined with standard `try/catch` blocks."),
            ("What is event delegation?", "Event delegation is a DOM event handling pattern where you attach a single event listener to a parent element rather than attaching individual listeners to multiple child elements. It leverages event bubbling, where events fired on child elements propagate up the DOM tree to the parent. By checking the `event.target` property within the parent's listener, you can determine which specific child triggered the event. This drastically reduces memory usage, improves performance, and automatically handles dynamically added child elements."),
            ("Explain Higher-Order Functions.", "A Higher-Order Function (HOF) is a function that either takes one or more functions as arguments (callbacks) or returns a function as its result. They are a core concept in functional programming. Common examples in JavaScript include array methods like `map()`, `filter()`, and `reduce()`, which take a function to process array elements. HOFs enable powerful abstractions, function composition, and cleaner, more declarative code by encapsulating repetitive iteration logic or configuration setups."),
            ("What is strict mode?", "Strict mode is a way to opt in to a restricted variant of JavaScript, introduced in ES5. Triggered by placing the string `'use strict';` at the top of a file or function, it intentionally changes normal JavaScript semantics. It eliminates some silent errors by throwing exceptions, prevents the creation of accidental global variables, prohibits the use of `this` pointing to the global object, and disables features that are confusing or poorly thought out (like `with` statements). Strict mode makes it easier to write secure, optimized JavaScript."),
            ("Explain the concept of pure functions.", "A pure function is a concept from functional programming. It must satisfy two primary conditions: 1) Given the same input, it will always return the same output. 2) It produces no side effects, meaning it does not modify any external state, mutate its arguments, or interact with the outside world (like DOM manipulation, API calls, or logging to console). Pure functions are highly predictable, incredibly easy to test, and safe to execute concurrently since they depend solely on their localized arguments.")
        ],
        "difficult": [
            ("How does garbage collection work in JavaScript?", "JavaScript utilizes automatic memory management via garbage collection. The primary algorithm used by modern engines (like V8) is 'Mark-and-Sweep'. Periodically, the garbage collector starts at the 'roots' (global object, local variables currently on the stack). It marks all objects reachable from these roots as 'alive'. It then recursively traverses object properties to mark further connected objects. Finally, the sweep phase reclaims memory for any objects in the heap that were not marked. This handles cyclical references effectively, though developers must still beware of memory leaks caused by lingering global variables, uncleared timers, or detached DOM nodes referenced in closures."),
            ("Explain the Temporal Dead Zone (TDZ).", "The Temporal Dead Zone (TDZ) refers to the period between the entering of a block scope and the actual execution of a variable's initialization line. While `let` and `const` declarations are technically hoisted to the top of their block scope, they remain completely uninitialized. Any attempt to access them before the engine evaluates the initialization statement results in a `ReferenceError`. This is a deliberate design choice in ES6 to catch errors related to using variables before they are explicitly defined, contrasting with `var` which silently returns `undefined`."),
            ("What are generators?", "Generators are special functions introduced in ES6 that can be paused and resumed, allowing them to yield multiple values over time instead of returning a single value. They are defined using the `function*` syntax. When called, a generator does not execute immediately but returns an iterator object. Calling `.next()` on this iterator executes the code until it encounters the `yield` keyword, which returns a value and pauses execution. Generators provide a powerful way to implement custom iterators, manage infinite sequences, and build complex asynchronous control flows (which heavily influenced `async/await`)."),
            ("How does the JavaScript engine execute code?", "Code execution happens in two main phases: the Creation/Compile Phase and the Execution Phase. During the Creation Phase, the engine parses the code, creates the Global Execution Context, sets up the memory space for variables and functions (hoisting), and determines the value of `this`. During the Execution Phase, the code is run line-by-line in the Call Stack. Modern engines like V8 use Just-In-Time (JIT) compilation, where the code is initially interpreted, but 'hot' code paths (frequently executed functions) are compiled down to optimized machine code on the fly for maximum performance."),
            ("Explain the module pattern.", "The Module Pattern is a design pattern used to encapsulate 'private' variables and methods, exposing only a public API. Before ES6 modules, this was typically achieved using Immediately Invoked Function Expressions (IIFEs) combined with closures. The IIFE creates a private scope where variables cannot be accessed externally. The function then returns an object containing the methods that require access to the private state. This pattern prevents global namespace pollution, manages dependencies, and provides robust data encapsulation. Today, native ES6 `import`/`export` syntax largely supersedes the need for IIFE-based modules."),
            ("What are WeakMap and WeakSet?", "`WeakMap` and `WeakSet` are ES6 data structures similar to `Map` and `Set`, but with critical differences designed for memory management. The keys in a `WeakMap` and the elements in a `WeakSet` must be objects, not primitive values. Most importantly, they hold 'weak' references to these objects. If there are no other references to an object used as a key in a `WeakMap`, that object will be garbage collected, and its entry in the `WeakMap` automatically removed. This makes them ideal for storing metadata for DOM nodes or managing cache without causing memory leaks."),
            ("How do Web Workers operate?", "Web Workers provide a standard way to run JavaScript in background threads. Since JavaScript is single-threaded, heavy computations on the main thread block UI rendering, causing the browser to freeze. Web Workers operate in a completely isolated execution context, meaning they do not have access to the DOM or the `window` object. Communication between the main thread and the worker happens exclusively through asynchronous message passing using the `postMessage()` API. They are essential for offloading intensive tasks like image processing, complex parsing, or heavy mathematical calculations to maintain a smooth user experience."),
            ("What is currying?", "Currying is an advanced functional programming technique where a function that takes multiple arguments is transformed into a sequence of nested functions, each taking a single argument. For example, a function `f(a, b, c)` is converted into `f(a)(b)(c)`. When the first function is called, it returns a new function expecting the next argument, and so on. Currying relies heavily on closures to remember the arguments passed in previous invocations. It is highly useful for partial application, creating reusable utility functions, and constructing highly composable declarative pipelines."),
            ("Explain Symbol data type.", "Introduced in ES6, `Symbol` is a primitive data type whose primary purpose is to generate unique, immutable identifiers. Calling `Symbol()` always returns a strictly unique value, even if created with the same descriptive string. Symbols are commonly used to add hidden or private properties to objects, as Symbol keys do not show up in standard iterations like `for...in` loops or `Object.keys()`. Furthermore, JavaScript utilizes 'Well-Known Symbols' (like `Symbol.iterator`) to expose and allow overriding of internal language behaviors, such as making an object iterable."),
            ("What is a proxy object?", "The `Proxy` object, introduced in ES6, enables you to create an intermediary wrapper for another object, allowing you to intercept and redefine fundamental operations for that object. These operations include property lookup, assignment, enumeration, function invocation, and more. A proxy accepts two arguments: the target object, and a 'handler' object defining 'traps' (methods like `get`, `set`, `has`) that intercept the operations. Proxies are incredibly powerful for creating reactive frameworks (like Vue 3's reactivity system), performing data validation, logging, and implementing complex access control.")
        ]
    },
    "python": {
         "easy": [
            ("What is the difference between a list and a tuple?", "In Python, both lists and tuples are sequence data types that can store collections of items. The primary difference is mutability. Lists, defined with square brackets `[]`, are mutable, meaning their elements can be changed, added, or removed after creation. Tuples, defined with parentheses `()`, are immutable, meaning they cannot be altered once created. Because of their immutability, tuples are generally faster, use less memory, and can be used as keys in dictionaries or elements in sets, whereas lists cannot."),
            ("What are decorators?", "Decorators are a powerful Python feature that allows you to modify or enhance the behavior of a function or class without permanently changing its source code. Syntactically, they are denoted by the `@` symbol placed above the function definition. Under the hood, a decorator is a higher-order function that takes the target function as an argument, wraps it in an inner function that adds some functionality (like timing, logging, or authentication checks), and returns the new wrapped function to be executed instead of the original."),
            ("Explain how memory is managed in Python.", "Memory management in Python is fully automated, handled by the Python Memory Manager and a built-in garbage collector. The memory manager handles allocations on a private heap space. For garbage collection, Python primarily uses reference counting, where an object is immediately deallocated once its reference count drops to zero. However, to resolve cyclical references (where two objects point to each other, preventing the count from reaching zero), Python also implements a generational garbage collector that periodically scans memory to detect and clean up these inaccessible reference cycles."),
            ("What is a dictionary?", "A dictionary in Python is an unordered (though ordered by insertion as of Python 3.7), mutable, and indexed collection of data stored as key-value pairs. Dictionaries are defined using curly braces `{}`. Keys must be unique and immutable data types (like strings, numbers, or tuples), while values can be of any type and can be duplicated. Under the hood, dictionaries are implemented as highly optimized hash tables, which makes retrieving, adding, or modifying values extremely fast, operating at an average time complexity of O(1)."),
            ("What does the `__init__` method do?", "The `__init__` method is a special magic method (or dunder method) in Python classes, acting similarly to a constructor in other object-oriented languages. It is automatically invoked immediately after a new instance of a class is created. Its primary purpose is to initialize the attributes of that specific object. It takes `self` as its first parameter—which represents the newly created instance—followed by any other arguments required to set up the object's initial state properly."),
            ("Explain list comprehensions.", "List comprehensions provide a concise, readable, and highly optimized syntax for creating new lists based on the values of existing iterables. The syntax is `[expression for item in iterable if condition]`. They condense traditional `for` loops and `append()` calls into a single line of code. Because list comprehensions are optimized at the C level within the Python interpreter, they are generally faster to execute than their equivalent `for` loop counterparts. They are heavily favored by Python developers for filtering and mapping data."),
            ("What is the difference between `is` and `==`?", "The `==` operator is an equality operator; it checks if the values of two operands are equal. The `is` operator is an identity operator; it checks if two operands refer to the exact same object in memory. For example, if you create two separate lists with identical contents, `list1 == list2` evaluates to True, but `list1 is list2` evaluates to False because they reside at different memory addresses. `is` is most commonly used to check if a variable is explicitly `None`."),
            ("What are lambda functions?", "Lambda functions, also known as anonymous functions, are small, unnamed functions defined using the `lambda` keyword. They are restricted to a single expression and cannot contain multiple statements or a complex block of code. The syntax is `lambda arguments: expression`. Lambda functions are typically used for short, throwaway operations where defining a full function with `def` would be overkill, frequently serving as arguments for higher-order functions like `map()`, `filter()`, and `sorted()`."),
            ("How do you handle exceptions in Python?", "Exceptions in Python are handled using the `try`, `except`, `else`, and `finally` blocks. Code that might raise an error is placed in the `try` block. If an error occurs, execution immediately jumps to the corresponding `except` block to handle it without crashing the program. The optional `else` block runs only if no exceptions were raised in the try block. The `finally` block executes regardless of whether an exception occurred, making it crucial for cleanup operations like closing files or releasing network connections."),
            ("What is `self` in classes?", "In Python, `self` is a strong convention (not a strict keyword) representing the instance of the class itself. When a method is called on an object, Python automatically passes the object reference as the first argument to the method. Using `self` inside class methods allows you to access or modify the attributes and other methods bound to that specific instance. Without `self`, variables defined within a method would be treated as temporary local variables rather than persistent instance attributes.")
        ],
        "moderate": [
            ("What is the Global Interpreter Lock (GIL)?", "The Global Interpreter Lock (GIL) is a mutex in CPython that protects access to Python objects, preventing multiple native threads from executing Python bytecodes simultaneously. Because CPython's memory management is not thread-safe, the GIL ensures that only one thread runs in the interpreter at any given time. While this makes single-threaded applications fast, it severely limits multi-threaded CPU-bound programs. To bypass the GIL and utilize multiple cores, developers must use the `multiprocessing` module, which spawns separate processes rather than threads."),
            ("Explain generators and the `yield` keyword.", "Generators are a simple and powerful tool for creating iterators. They are written like regular functions but use the `yield` statement to return data. Every time `yield` is encountered, the generator pauses its execution, saves all its local state, and returns the value to the caller. When `.next()` is called again, it resumes right where it left off. This lazy evaluation makes generators extremely memory efficient for processing massive datasets or infinite streams, as they generate one item at a time instead of loading the entire sequence into memory."),
            ("What is duck typing?", "Duck typing is a concept related to dynamic typing, derived from the phrase 'If it walks like a duck and quacks like a duck, it must be a duck.' In Python, the suitability of an object to be used in a specific context is determined by the presence of certain methods and properties, rather than the object's actual class type. This means you don't need to explicitly check an object's type or implement specific interfaces; as long as the object implements the required methods (like `__len__` or `read`), the code will execute successfully."),
            ("Explain the difference between deep copy and shallow copy.", "A shallow copy creates a new object, but instead of copying the nested objects, it inserts references to the original nested objects. Modifying a nested mutable object in a shallow copy will affect the original. A deep copy, created using `copy.deepcopy()`, constructs a completely new collection object and then recursively inserts copies of the nested objects found in the original. Modifying nested objects in a deep copy leaves the original entirely unaffected, isolating state changes completely."),
            ("What are magic methods (dunder methods)?", "Magic methods, flanked by double underscores (dunder), are special methods that define how Python objects behave with built-in operations. For example, `__init__` handles object initialization, `__str__` and `__repr__` define string representations for printing, `__add__` defines behavior for the `+` operator, and `__len__` defines behavior for the `len()` function. By overriding these methods, developers can enable custom classes to seamlessly emulate and integrate with Python's built-in types and operators, enabling operator overloading."),
            ("How does \*args and \*\*kwargs work?", "`*args` and `**kwargs` are conventions used to pass a variable number of arguments to a function. `*args` allows a function to accept any number of positional arguments, packing them into a tuple accessible within the function. `**kwargs` stands for keyword arguments and allows the function to accept any number of named arguments, packing them into a dictionary. They provide immense flexibility, especially when writing wrappers, decorators, or class inheritance where the exact signature of the child function is unknown."),
            ("Explain the concept of metaclasses.", "Metaclasses are the 'classes of classes'. Just as an object is an instance of a class, a class itself is an instance of a metaclass (by default, the `type` metaclass). Metaclasses allow you to intercept the creation of a class, enabling you to automatically modify class definitions, add or remove methods, or enforce specific coding standards at the time the class is defined, before any instances are created. They are an advanced feature, heavily used in ORMs (like Django's) to map class attributes to database columns."),
            ("What is context management and the `with` statement?", "Context managers define the setup and teardown logic for allocating and releasing resources. They are typically invoked using the `with` statement. The most common use case is file I/O operations (`with open('file.txt') as f:`), which guarantees that the file is automatically and safely closed after the block executes, even if an exception occurs. Under the hood, a context manager is implemented by defining the `__enter__` and `__exit__` magic methods within a class, providing a clean alternative to verbose try/finally blocks."),
            ("How does Python resolve method resolution order (MRO)?", "Method Resolution Order (MRO) is the sequence in which Python looks for a method in a hierarchy of classes, especially critical in multiple inheritance scenarios. Python uses the C3 Linearization algorithm to construct the MRO. This algorithm ensures that a class always precedes its parents, and if multiple parents exist, it preserves the order in which they were specified in the class definition. Developers can inspect the exact MRO of a class by accessing the `__mro__` attribute or calling the `mro()` method."),
            ("What are virtual environments?", "Virtual environments are isolated directories containing a specific Python installation and its own set of installed packages and dependencies. They are crucial because Python packages are globally installed by default. If project A requires Django 2.0 and project B requires Django 3.0, a global installation will cause conflicts. Using tools like `venv` or `virtualenv`, developers create isolated sandboxes for each project, ensuring dependency resolution does not bleed across projects, maintaining reproducible builds.")
        ],
        "difficult": [
            ("How would you optimize a slow Python application?", "Optimizing Python requires a multi-faceted approach. First, profiling tools like `cProfile` and `line_profiler` must be used to identify precise bottlenecks, rather than guessing. Algorithmic inefficiencies (e.g., O(N^2) loops) should be refactored using appropriate data structures like sets or dictionaries. I/O-bound bottlenecks should be addressed using asynchronous programming (`asyncio`) or multithreading. For severe CPU-bound limitations, developers can use `multiprocessing`, utilize C-extensions via Cython, or run the code on faster alternative interpreters like PyPy. Aggressive caching using `functools.lru_cache` or Redis is also critical."),
            ("Explain the inner workings of Python dictionaries.", "Python dictionaries are implemented as highly optimized hash tables. When inserting a key-value pair, the built-in `hash()` function computes a hash value for the key. This hash value is masked to determine an index in an underlying sparse array (the bucket). If a hash collision occurs (two keys map to the same bucket), Python uses an open addressing strategy called pseudo-random probing to find the next available slot. Because they are implemented in highly optimized C code, average lookups and insertions are O(1)."),
            ("What is `asyncio` and how does it differ from multithreading?", "`asyncio` is a library to write concurrent code using the `async/await` syntax. While multithreading uses OS-level threads to achieve concurrency (preemptive multitasking managed by the OS), `asyncio` runs entirely on a single thread utilizing an Event Loop (cooperative multitasking). Coroutines explicitly yield control back to the Event Loop when awaiting an I/O operation, allowing other coroutines to run. This drastically reduces the memory overhead of spawning OS threads and eliminates complex race conditions, making it vastly superior for scaling highly concurrent network applications."),
            ("Explain Python's garbage collection mechanisms in deep detail.", "Python memory management relies primarily on Reference Counting. Every object maintains a count of references pointing to it; when this drops to zero, the memory is instantly freed. However, reference counting fails with cyclic references (e.g., Object A references Object B, and Object B references A). To resolve this, Python runs a secondary Generational Garbage Collector. It tracks container objects (like lists, dicts) across three 'generations'. Objects surviving collection are promoted to older generations, which are scanned less frequently. The collector identifies unreachable cycles and sweeps them, ensuring memory is fully reclaimed."),
            ("What are descriptors in Python?", "Descriptors are the underlying mechanism behind properties, methods, static methods, and super() in Python. A descriptor is any object that defines at least one of the following magic methods: `__get__`, `__set__`, or `__delete__`. When a class attribute is assigned a descriptor, accessing or modifying that attribute delegates the operation to the descriptor's methods. Descriptors provide a powerful, low-level way to intercept attribute access, enforce type checking, or compute values dynamically upon access, forming the backbone of advanced object-oriented behaviors."),
            ("How does Cython work and when would you use it?", "Cython is an optimizing static compiler for both the Python language and the extended Cython programming language. It allows developers to write Python-like code augmented with optional C-type declarations. Cython translates this code into highly optimized C/C++ code, which is then compiled into a native extension module. It is used when Python's execution speed is unacceptable for CPU-bound tasks. By defining static C types for variables and bypassing the Python object overhead and the GIL, Cython can yield performance improvements of 10x to 100x over pure Python."),
            ("Explain the concept of monkey patching.", "Monkey patching refers to dynamically modifying or extending a class or module at runtime. In Python, because functions and classes are first-class objects and dictionaries under the hood, you can easily overwrite a class method or a module function with a custom implementation during execution. While powerful for hot-fixing bugs in third-party libraries or mocking behavior in unit tests without altering the original source code, it is generally discouraged in production code because it creates silent, unpredictable behavior changes that are extremely difficult to trace and maintain."),
            ("What is the difference between `@staticmethod` and `@classmethod`?", "Both decorators define methods that belong to a class rather than an instance. A `@classmethod` takes the class itself (`cls`) as its implicit first argument, allowing it to modify class state that applies across all instances or to serve as an alternative constructor (factory method) returning an instance. A `@staticmethod` takes neither the instance (`self`) nor the class (`cls`) as an implicit argument. It acts as a regular utility function that is logically scoped to the class namespace, but it cannot modify object or class state."),
            ("How do closures work in Python?", "A closure occurs when a nested inner function captures and remembers the local variables from its enclosing outer function's scope, even after the outer function has finished executing. In Python, this is achieved by storing these captured variables in the inner function's `__closure__` attribute. Closures are vital for maintaining state without resorting to global variables or creating full classes, and they form the fundamental architectural basis for implementing decorators, data hiding, and functional programming patterns like currying."),
            ("What are type hints and how does `mypy` use them?", "Introduced in Python 3.5, type hints allow developers to optionally specify the expected data types for function arguments and return values using annotations. Crucially, the Python runtime ignores these hints entirely—they do not affect execution speed or enforce strict types. However, static analysis tools like `mypy` can scan the codebase before execution, utilizing these hints to detect type inconsistencies and potential bugs. This provides the safety and tooling benefits of statically typed languages while preserving Python's dynamic nature at runtime.")
        ]
    },
    "react": {
        "easy": [
            ("What is React?", "React is a declarative, efficient, and flexible JavaScript library created by Facebook for building dynamic user interfaces. It focuses entirely on the View layer of the MVC pattern. Instead of manipulating the DOM directly, developers write reusable components that describe what the UI should look like for a given state. React takes care of efficiently updating and rendering the right components when your data changes, making code more predictable and easier to debug."),
            ("What is JSX?", "JSX (JavaScript XML) is a syntax extension for JavaScript popularized by React. It allows developers to write HTML-like markup directly inside their JavaScript files. While it looks like HTML, JSX is actually compiled down to regular JavaScript function calls (specifically `React.createElement()`) by tools like Babel before reaching the browser. JSX makes writing React components highly intuitive, as it keeps the rendering logic and UI markup coupled together in the same file."),
            ("What is the Virtual DOM?", "The Virtual DOM is a lightweight, in-memory representation of the actual DOM. Direct DOM manipulation is notoriously slow. React solves this by keeping a virtual copy. When state changes, React updates the Virtual DOM first, compares it to the previous version using a 'diffing' algorithm, calculates the absolute minimum number of changes required, and applies only those specific changes to the real DOM in a single batch. This drastically improves performance."),
            ("What are components in React?", "Components are the core building blocks of any React application. They are independent, reusable pieces of UI. You can think of components as custom JavaScript functions that accept inputs (called 'props') and return React elements describing what should appear on the screen. By breaking complex UIs down into small, isolated, and composable components, developers can build scalable applications that are easier to maintain, test, and understand."),
            ("What is state in React?", "`state` is a built-in React object used to contain data or information about the component. Unlike `props` which are passed down from a parent and are immutable, `state` is entirely managed locally within the component and can change over time. When a component's state changes (typically via the `setState` function or the `useState` hook), React automatically re-renders the component and its children to reflect the new data in the UI."),
            ("What are props?", "Props (short for properties) are the mechanism by which data is passed down from a parent component to a child component in React. Props are strictly read-only; a child component must never modify its own props, adhering to the concept of unidirectional data flow. Props allow components to be dynamic and reusable, as the same component can be rendered multiple times with different data passed in as props."),
            ("What is the difference between state and props?", "The primary difference lies in data ownership and mutability. `props` are arguments passed down into a component from its parent, and they are immutable (read-only) by the receiving component. `state`, on the other hand, is managed internally by the component itself, acting as its personal memory. State is mutable and is used to handle dynamic changes like user inputs or API responses. When state changes, the component re-renders."),
            ("What are React Hooks?", "Introduced in React 16.8, Hooks are special functions that allow you to 'hook into' React state and lifecycle features from functional components. Before hooks, managing state required writing class components. With hooks like `useState` and `useEffect`, functional components became fully capable of managing state and side effects, leading to cleaner, more concise code and completely shifting the React ecosystem away from class-based architecture."),
            ("What does `useState` do?", "`useState` is a fundamental React Hook that allows functional components to manage local state. When called, it returns an array containing two elements: the current state value, and a setter function used to update that state. When the setter function is invoked with a new value, React schedules a re-render of the component. It takes an initial state value as its only argument, which is only used during the very first render."),
            ("What is a React Fragment?", "A React Fragment (`<React.Fragment>` or the shorthand syntax `<></>`) is a feature that allows developers to group a list of multiple children elements without adding an extra node to the actual DOM. React components must return a single parent element. Instead of wrapping siblings in an unnecessary `<div>`—which can break CSS grid/flexbox layouts or bloat the DOM—Fragments cleanly group the elements transparently.")
        ],
        "moderate": [
            ("Explain the `useEffect` hook.", "The `useEffect` hook enables functional components to perform side effects, such as data fetching, manual DOM manipulation, or setting up subscriptions. It serves the same purpose as `componentDidMount`, `componentDidUpdate`, and `componentWillUnmount` in React classes unified into a single API. It takes a callback function and an optional dependency array. If the dependency array is empty `[]`, the effect runs only once on mount. If variables are included, it runs whenever those variables change. Returning a function from the effect handles necessary cleanup before the component unmounts."),
            ("What is the Context API?", "The Context API is a built-in React feature used to share data globally across an entire component tree without having to pass props down manually at every single level (a problem known as prop drilling). It involves creating a Context object, providing its value via a `<Context.Provider>` at a high level in the tree, and consuming that value deep within the tree using the `useContext` hook. It is ideal for global themes, user authentication state, or language preferences."),
            ("What is prop drilling and how do you avoid it?", "Prop drilling occurs when you need to pass data from a high-level parent component to a deeply nested child component, requiring you to pass the prop through multiple intermediate components that do not actually need the data themselves. This pollutes the codebase and makes components tightly coupled. It is avoided by using the React Context API for global state, implementing external state management libraries like Redux or Zustand, or utilizing component composition patterns (passing components as children)."),
            ("What are controlled vs. uncontrolled components?", "In a controlled component, form data is handled entirely by React state. The input's `value` is tied to a state variable, and changes are handled by an `onChange` event updating that state, making React the single source of truth. In an uncontrolled component, form data is handled directly by the DOM itself. Instead of state, you use a `ref` (via `useRef`) to pull the value from the DOM only when needed. Controlled components are the recommended standard in React for robust validation."),
            ("Explain how React's reconciliation works.", "Reconciliation is React's algorithm for diffing the Virtual DOM tree to determine what needs to be updated in the real DOM. When state changes, a new tree is built. React compares the new tree with the old tree. If the element types are different, React tears down the old tree completely. If they are the same type, React updates only the changed attributes. For lists of children, React uses 'keys' to efficiently match elements between trees, ensuring items are reordered rather than destroyed and recreated."),
            ("Why are keys necessary in React lists?", "When rendering lists of elements, React requires a unique `key` prop for each item. Keys help React uniquely identify which items have changed, been added, or been removed. During reconciliation, React uses keys to match children in the original tree with children in the subsequent tree. Without stable, unique keys (using array indices is heavily discouraged), React may mix up component state or unnecessarily recreate DOM nodes, leading to severe performance degradation and unpredictable UI bugs."),
            ("What is `useRef` used for?", "The `useRef` hook returns a mutable ref object whose `.current` property is initialized to the passed argument. Crucially, mutating the `.current` property does not trigger a component re-render. It is primarily used for directly accessing DOM elements (like focusing an input). However, it is also highly useful for storing mutable values that persist across renders but shouldn't trigger re-renders, acting essentially as instance variables for functional components (e.g., storing a timer ID to clear it later)."),
            ("What is `useMemo` and `useCallback`?", "Both hooks are used for performance optimization. `useMemo` is used to memoize (cache) the result of an expensive calculation, only recomputing the value when one of its dependencies changes. `useCallback` is used to memoize a function definition, preventing the function from being recreated on every render. This is particularly crucial when passing callbacks as props to optimized child components wrapped in `React.memo`, preventing unnecessary re-renders of the child by maintaining referential equality of the function."),
            ("What is a Higher-Order Component (HOC)?", "A Higher-Order Component (HOC) is an advanced React pattern used for reusing component logic. Technically, an HOC is a function that takes a component as an argument and returns a new, enhanced component. They are not part of the React API but emerge from its compositional nature. HOCs were heavily used in class-based React for cross-cutting concerns like authentication checks or injecting data, though custom Hooks have largely superseded them in modern functional React."),
            ("Explain React's strict mode.", "`<React.StrictMode>` is a tool for highlighting potential problems in an application. It does not render any visible UI. It activates additional checks and warnings for its descendants. Specifically, in development mode, it intentionally double-invokes components, side effects, and state updater functions to help developers identify impure components and unintended side effects. It also warns about the usage of deprecated APIs and legacy string refs, ensuring the codebase is ready for concurrent rendering features.")
        ],
        "difficult": [
            ("Explain how React Fiber works.", "React Fiber is the complete rewrite of React's core reconciliation algorithm, introduced in React 16. The primary goal of Fiber is to enable incremental rendering—the ability to split rendering work into chunks and spread it out over multiple frames. Instead of the old recursive, blocking approach, Fiber uses an iterative architecture based on a linked list of 'fiber nodes'. This allows React to pause rendering work, yield control back to the browser's main thread to handle high-priority events (like user input or animations), and resume rendering later, drastically improving the perceived performance and responsiveness of complex UIs."),
            ("How do you manage complex state in a React application?", "For localized complex state, `useReducer` is often preferred over multiple `useState` calls, as it handles state transitions via predictable reducer functions based on actions. As data needs to be shared across many disconnected components, the Context API provides a native solution, though it can cause unnecessary re-renders if not heavily optimized with `useMemo`. For massive, enterprise-scale applications, external libraries like Redux (using Redux Toolkit) are standard, providing a centralized, immutable store and predictable unidirectional data flow. Modern alternatives like Zustand or Jotai offer simpler, atomic state management with less boilerplate."),
            ("Explain Server-Side Rendering (SSR) vs. Client-Side Rendering (CSR).", "In traditional Client-Side Rendering (CSR), the server sends a minimal HTML file and a massive JavaScript bundle; the browser downloads the JS and React builds the UI dynamically. This results in slower initial load times and poor SEO. Server-Side Rendering (SSR), utilized by frameworks like Next.js, executes React on the server and sends fully formed, populated HTML to the browser. The user sees content instantly. Once the JavaScript loads, React 'hydrates' the static HTML, attaching event listeners to make it interactive. SSR heavily optimizes First Contentful Paint and SEO at the cost of server load."),
            ("What are React Server Components (RSC)?", "React Server Components are a paradigm shift introduced recently. Unlike traditional SSR, Server Components render exclusively on the server and never send their JavaScript code to the client. This dramatically reduces the client bundle size. They seamlessly integrate with Client Components, allowing developers to fetch data securely on the server (accessing databases directly) while leaving interactive parts (like buttons or state) to Client Components. RSCs allow for a highly optimized architecture where heavy lifting happens server-side, blurring the lines between backend and frontend."),
            ("How do you handle performance bottlenecks in a large React app?", "Performance optimization involves multiple strategies. First, use the React Profiler to identify components that re-render too often. Implement `React.memo` to prevent re-renders of components whose props haven't changed. Use `useMemo` for expensive calculations and `useCallback` to maintain referential equality of functions passed as props. Implement code-splitting and lazy loading (`React.lazy` and `Suspense`) to break the JavaScript bundle into smaller chunks loaded on demand. Finally, ensure lists are virtualized (using libraries like `react-window`) so only the items currently visible in the viewport are rendered to the DOM."),
            ("What is concurrent mode in React?", "Concurrent features in React (enabled fully in React 18) allow React to prepare multiple versions of the UI simultaneously behind the scenes. Unlike synchronous rendering, which is uninterruptible, concurrent rendering allows React to start rendering an update, pause it to handle a more urgent user interaction, and resume or discard the previous work. Features like `startTransition` allow developers to mark certain state updates as non-urgent, keeping the UI highly responsive during heavy data processing or screen transitions."),
            ("Explain custom hooks and their architectural value.", "Custom Hooks are standard JavaScript functions whose names start with 'use' and which call other built-in React Hooks. They are the primary mechanism for extracting and reusing stateful logic across multiple components. Unlike Higher-Order Components or Render Props, custom hooks allow you to share logic without altering your component hierarchy or adding wrapper hell. By isolating complex logic (like fetching data, managing web sockets, or handling form state) into custom hooks, component files become strictly declarative, significantly improving readability and testability."),
            ("What is the difference between `useEffect` and `useLayoutEffect`?", "Both hooks handle side effects, but their timing is strictly different. `useEffect` runs asynchronously after the browser has painted the screen, meaning it won't block visual updates. It is the default choice for data fetching or setting up subscriptions. `useLayoutEffect` fires synchronously immediately after DOM mutations, but before the browser has a chance to paint. It is used exclusively when you need to read the layout from the DOM (e.g., getting the scroll position or element dimensions) and synchronously re-render to prevent visual flickering."),
            ("How does Redux work under the hood with React?", "Redux relies on a single immutable global object called the Store. UI components dispatch 'Actions' (plain objects describing what happened) to the Store. The Store passes the Action and the current State to a 'Reducer', a pure function that calculates and returns a completely new State object. The React-Redux library connects this system to React components. Using the `useSelector` hook, components subscribe to specific slices of the state. When the Redux state updates, `useSelector` forces the connected components to re-render, ensuring the UI remains perfectly synced with the global store."),
            ("What are Error Boundaries in React?", "Error Boundaries are React components that catch JavaScript errors anywhere in their child component tree, log those errors, and display a fallback UI instead of crashing the entire component tree and displaying a blank white screen. They are implemented using class components by defining the `static getDerivedStateFromError()` or `componentDidCatch()` lifecycle methods. It's crucial to note that Error Boundaries do not catch errors inside event handlers, asynchronous code, or server-side rendering; they specifically catch errors thrown during the render phase and in lifecycle methods.")
        ]
    },
    "system-design": {
        "easy": [
            ("What is System Design?", "System Design is the process of defining the architecture, modules, interfaces, and data for a system to satisfy specified requirements. It bridges the gap between raw business requirements and software implementation. In modern terms, it focuses on designing distributed systems—creating scalable, reliable, and highly available architectures that handle massive traffic and data. It involves making deliberate trade-offs, deciding where to place databases, caches, load balancers, and how microservices communicate to ensure the system is performant and fault-tolerant."),
            ("What is the difference between vertical and horizontal scaling?", "Vertical scaling (scaling up) means adding more power—CPU, RAM, or storage—to a single existing machine. It is straightforward but has a hard physical limit and creates a single point of failure. Horizontal scaling (scaling out) involves adding more machines to a resource pool to share the workload. Horizontal scaling provides near-infinite scalability and high availability, but it vastly increases system complexity, requiring load balancers, distributed databases, and stateless application designs."),
            ("What is a Load Balancer?", "A Load Balancer is a critical network component that sits in front of backend servers. It acts as a reverse proxy, distributing incoming client requests across multiple servers to ensure no single server becomes overwhelmed. This maximizes throughput, minimizes response time, and ensures high availability. If a server goes down, the load balancer automatically redirects traffic to healthy servers. Load balancers can operate at Layer 4 (Transport) or Layer 7 (Application) of the OSI model."),
            ("What is Caching?", "Caching is the practice of storing frequently accessed data in a temporary, high-speed storage layer (usually RAM) so that future requests for that data are served exponentially faster. Instead of querying a slow, disk-based database repeatedly for the same information, the application checks the cache first. Popular caching technologies include Redis and Memcached. Caching dramatically reduces latency and database load, though it introduces complexity regarding cache invalidation (ensuring data doesn't become stale)."),
            ("What is a Content Delivery Network (CDN)?", "A Content Delivery Network (CDN) is a geographically distributed network of proxy servers. Its primary purpose is to deliver static content (like images, videos, CSS, and JavaScript files) to users faster by caching this data at 'edge servers' located close to the user's physical location. When a user requests an image, it is served by the nearest edge server rather than traversing the globe to the origin server, significantly reducing latency and protecting the origin server from massive traffic spikes."),
            ("What is latency vs throughput?", "Latency is the time it takes for a single data packet to travel from the source to the destination and back (often measured in milliseconds). It is a measure of delay. Throughput, on the other hand, is the amount of data successfully transferred or the number of requests processed over a given period (e.g., requests per second or Mbps). In system design, architects strive to minimize latency (making the system fast) while maximizing throughput (making the system capable of handling heavy volume)."),
            ("Explain the difference between SQL and NoSQL databases.", "SQL (Relational) databases, like MySQL or PostgreSQL, store data in structured tables with strict schemas and enforce relationships via foreign keys. They use structured query language and guarantee ACID properties, making them ideal for complex queries and transactional integrity. NoSQL databases, like MongoDB or Cassandra, are non-relational and store data in flexible formats like JSON documents, key-value pairs, or wide columns. NoSQL scales horizontally much easier and is preferred for unstructured data, rapid development, and handling massive, high-velocity workloads."),
            ("What is a microservices architecture?", "Microservices architecture is a design pattern where a massive application is broken down into small, loosely coupled, independently deployable services. Each service handles a specific business capability (e.g., billing, user authentication) and communicates with other services over a network, typically via REST APIs or message brokers. This contrasts with monolithic architecture, where everything is bundled into one codebase. Microservices allow different teams to work independently, scale specific parts of the app independently, and use different technology stacks for different services."),
            ("What is database indexing?", "An index in a database is a specialized data structure (often a B-Tree) that improves the speed of data retrieval operations on a table at the cost of additional storage space and slightly slower writes. Much like the index at the back of a book, it allows the database engine to quickly locate the exact rows satisfying a WHERE clause without having to scan every single row in the entire table (a full table scan). Proper indexing is the single most effective way to optimize read-heavy database performance."),
            ("What is API Rate Limiting?", "Rate Limiting is a defensive mechanism used to control the rate of traffic sent to an API or service. It restricts the number of requests a user or IP address can make within a specified time window. This is crucial for preventing denial-of-service (DDoS) attacks, brute-force attacks, and preventing a single poorly written client script from overwhelming the servers and degrading the experience for all other users. Common algorithms include Token Bucket and Leaky Bucket.")
        ],
        "moderate": [
            ("Explain the CAP Theorem.", "The CAP Theorem asserts that a distributed data store can provide only two of the following three guarantees simultaneously: Consistency (every read receives the most recent write), Availability (every request receives a non-error response), and Partition Tolerance (the system continues to operate despite network failures dropping messages). Because network partitions are inevitable in real-world distributed systems, designers cannot sacrifice Partition Tolerance. Therefore, the real choice is between Consistency (CP systems, which reject writes during partitions to prevent split-brain) and Availability (AP systems, which accept writes but return potentially stale data)."),
            ("How does database sharding work?", "Database sharding is an architectural pattern for horizontally scaling databases. It involves splitting a massive logical database table into smaller, separate, independently managed tables called 'shards', which are distributed across multiple physical servers. The system uses a 'shard key' (e.g., user_id) to determine mathematically which shard holds the data. Sharding dramatically increases read/write throughput and storage capacity. However, it introduces immense complexity: joining data across different shards is difficult, and poorly chosen shard keys can lead to 'hotspotting', where one server takes most of the traffic."),
            ("What is consistent hashing?", "Consistent hashing is an advanced hashing algorithm used extensively in distributed systems and load balancing (like DynamoDB or Memcached). In standard hashing, adding or removing a server invalidates almost all existing mappings, causing massive data reshuffling. Consistent hashing maps both the data keys and the servers onto a virtual 'hash ring'. A key is assigned to the first server it encounters moving clockwise on the ring. When a server is added or removed, only the keys immediately adjacent to it are affected, minimizing data movement and allowing graceful scaling."),
            ("Explain asynchronous processing and message queues.", "In high-traffic systems, synchronous processing (waiting for a task to finish before responding) causes latency and timeouts. Asynchronous processing solves this by decoupling tasks. When a user triggers a heavy task (like video processing), the web server immediately drops a message into a Message Queue (like RabbitMQ or Apache Kafka) and returns a fast response to the user. Background worker services monitor the queue, pull messages off at their own pace, and process the heavy tasks without impacting the responsiveness of the main web servers."),
            ("What are the differences between Long Polling, WebSockets, and Server-Sent Events (SSE)?", "These are techniques for enabling real-time client-server communication. Long Polling involves the client requesting data and the server holding the connection open until new data arrives; it's resourceful but has overhead. Server-Sent Events (SSE) establish a persistent, unidirectional connection where the server pushes updates to the client (ideal for news feeds or stock tickers). WebSockets provide a full-duplex, persistent, bidirectional connection over a single TCP socket, allowing instantaneous, low-latency communication in both directions, making it the gold standard for chat applications and multiplayer games."),
            ("What is the difference between a forward proxy and a reverse proxy?", "A forward proxy sits in front of client machines. When a client requests an internet resource, the proxy intercepts it, potentially masking the client's IP, filtering content, or bypassing firewalls. It protects the client. A reverse proxy sits in front of web servers. When internet traffic approaches the servers, the reverse proxy intercepts it, handling load balancing, SSL termination, caching, and security (like Web Application Firewalls). It protects and scales the servers. Nginx and HAProxy are standard reverse proxies."),
            ("How do you ensure data consistency in a distributed system?", "Ensuring consistency across microservices with separate databases is notoriously difficult because standard ACID transactions don't span across multiple services. Instead, architects rely on Eventual Consistency. The most common pattern is the 'Saga Pattern'. A Saga is a sequence of local transactions; each service updates its database and publishes an event triggering the next service. If a step fails, compensating transactions are triggered to undo the preceding steps. This ensures data integrity without locking resources across the network."),
            ("Explain Leader Election in distributed systems.", "In a distributed system, you often have a cluster of identical nodes. To coordinate tasks, avoid race conditions, or manage writes to a database partition, one node must be designated as the 'Leader' (or Master), while the rest act as 'Followers'. Leader Election is the consensus algorithm used to agree on who the leader is. If the leader crashes, the followers automatically communicate and elect a new leader to ensure the system remains available. Algorithms like Paxos, Raft, or services like Apache ZooKeeper are used to manage this complex coordination."),
            ("What is an API Gateway?", "An API Gateway is a server that acts as a single, unified entry point into a microservices architecture. Instead of clients calling dozens of different microservices directly, they make a single call to the Gateway. The Gateway routes the request to the appropriate backend service. Crucially, the Gateway handles cross-cutting concerns that you don't want to duplicate in every microservice, such as authentication, authorization, SSL termination, rate limiting, logging, and response aggregation, simplifying the backend architecture."),
            ("Explain the concept of 'Idempotency' in API design.", "An API endpoint is idempotent if making the identical request multiple times produces the exact same result and system state as making the request once. This is critical in distributed systems where network failures are common. If a client sends a payment request but doesn't receive a response due to a network timeout, it will retry the request. If the API is not idempotent, the user gets charged twice. Idempotency is usually implemented by having the client generate and send a unique 'idempotency key' with the request; the server checks if it has already processed that key.")
        ],
        "difficult": [
            ("Design a system like Twitter (X).", "Designing Twitter requires addressing massive read-heavy traffic and real-time updates. The core feature is the News Feed. A push-based 'Fanout' approach is typical: when a user tweets, the system asynchronously pushes that tweet into the pre-computed feed caches (using Redis) of all their followers via message queues. However, for celebrities with millions of followers, pushing to millions of caches causes a bottleneck. A hybrid approach is used: standard users use push, while celebrity tweets use pull (the feed fetches them dynamically on load). Data is heavily sharded by user ID, and a CDN handles media delivery."),
            ("How would you design a URL Shortener like Bitly?", "A URL shortener's core is generating a unique, short alias for a long URL and redirecting users quickly. The scale involves billions of URLs, requiring high availability and low latency reads. For generating aliases, a centralized Key Generation Service (KGS) is used. The KGS pre-generates random 7-character base62 strings and stores them in a database, handing them out to web servers rapidly upon request, preventing collisions. Read traffic is globally distributed and heavily cached using Memcached/Redis. 301 vs 302 HTTP redirects must be considered; 301 caches the redirect in the browser, saving server load, but 302 allows tracking analytics."),
            ("Design a rate limiter for an API.", "Designing a distributed rate limiter requires tracking request counts across multiple web servers. A Redis cluster is ideal due to its fast in-memory operations. Algorithms vary. The 'Token Bucket' algorithm is standard: a bucket holds tokens replenished at a set rate; each request consumes a token. If empty, the request is dropped. To implement this distributedly, Redis uses a Lua script to fetch the current token count, check limits, and decrement atomically, preventing race conditions from concurrent requests. A fallback mechanism is required so the API doesn't fail if the rate limiter cache goes down."),
            ("Explain how distributed consensus algorithms like Raft work.", "In distributed systems, ensuring multiple nodes agree on a single state despite failures is the Consensus Problem. Raft solves this by breaking consensus into Leader Election and Log Replication. Time is divided into terms. Nodes start as followers. If they hear no heartbeat from a leader, they become candidates and request votes. The node with the majority wins. All client writes go to the Leader. The Leader appends the command to its log and sends an AppendEntries RPC to followers. Once a majority of followers acknowledge, the Leader commits the entry, applies it to its state machine, and replies to the client, guaranteeing fault-tolerant consistency."),
            ("How do you handle 'Hotspotting' in a distributed database?", "Hotspotting occurs in sharded databases when a vast majority of read or write traffic hits a single shard, overwhelming it while other shards sit idle. This usually stems from a poorly chosen shard key. If sharding by timestamp, all new writes hit the 'latest' shard. To fix this, developers use composite shard keys (combining timestamp with a random hash) to distribute writes evenly. For read-heavy hotspots (e.g., a viral tweet), the solution is aggressive caching at the application layer or edge (CDN) to intercept traffic before it hits the database shard."),
            ("Design a system for a real-time collaborative editor like Google Docs.", "A collaborative editor requires real-time, low-latency synchronization of text between multiple users, resolving conflicting edits seamlessly. The core algorithm used is Operational Transformation (OT) or Conflict-free Replicated Data Types (CRDTs). When User A types, the operation is sent to the server via WebSockets. If User B types simultaneously, the server uses OT to transform the operations mathematically so that applying them in any order results in the exact same final document state. The architecture relies on an active session server managing WebSocket connections, backed by an in-memory datastore (Redis) for fast sync, and asynchronous backups to persistent storage."),
            ("Explain the architecture of a robust Search Engine like Elasticsearch.", "Elasticsearch is a distributed, RESTful search and analytics engine built on Apache Lucene. At its core, it relies on an Inverted Index. Instead of mapping documents to words (like a normal database), an inverted index maps words to the documents containing them, enabling lightning-fast full-text searches. Architecturally, an Elasticsearch index is divided into Shards. Each shard is a self-contained Lucene index hosted on a node. To ensure high availability, shards have Replicas. When a search query arrives, a coordinating node scatters the query to all relevant shards, gathers the results, sorts them by relevance (using TF-IDF or BM25 scoring), and returns the final list."),
            ("How would you design a distributed ID generation system (like Twitter Snowflake)?", "Generating unique, sortable IDs in a distributed system without a central bottleneck is complex. Using a database auto-increment fails at scale. Twitter Snowflake generates 64-bit integer IDs. The first 41 bits represent a timestamp (giving sortability by time). The next 10 bits represent the machine/worker ID, allowing 1024 unique machines to generate IDs concurrently without network communication. The final 12 bits are a sequence number, allowing a single machine to generate 4096 unique IDs per millisecond. This decentralized approach guarantees absolute uniqueness, rough time-ordering, and massive throughput."),
            ("Design a geographically distributed notification system.", "A notification system at scale must handle millions of pushes (iOS, Android, SMS, Email) reliably. Components include Notification Servers that receive requests, validate them, and push payloads into Message Queues (Kafka). Separate worker pools pull from these queues and interface with third-party APIs (APNs for Apple, FCM for Firebase). To handle global distribution, data centers in different regions process their localized queues. To prevent duplicate notifications (due to retry mechanisms), the workers implement idempotency checks using Redis. User preferences (opt-outs) must be cached heavily to avoid hitting the database on every notification attempt."),
            ("What is a Bloom Filter and where is it used in System Design?", "A Bloom Filter is a space-efficient probabilistic data structure used to test whether an element is a member of a set. It uses multiple hash functions to set bits in a bit array. It can yield false positives (saying an item is in the set when it isn't), but never false negatives (if it says an item is not in the set, it definitely is not). They are heavily used to save massive database lookups. For example, a web crawler uses a Bloom Filter to instantly check if a URL has already been visited, or a database uses it to quickly determine if a row exists on disk before doing an expensive read operation.")
        ]
    },
    "dsa": {
        "easy": [
            ("What is an Array?", "An array is the simplest and most widely used data structure. It is a collection of items stored at contiguous memory locations. The idea is to store multiple items of the same type together, allowing for calculation of the position of each element by simply adding an offset to a base value. Because memory is contiguous, accessing any element by its index is extremely fast, taking O(1) constant time. However, inserting or deleting elements (especially at the beginning or middle) is slow, taking O(N) time, as it requires shifting all subsequent elements in memory."),
            ("What is a Linked List?", "A linked list is a linear data structure, but unlike an array, its elements are not stored in contiguous memory locations. Instead, elements are linked using pointers. Each element, called a node, contains two parts: the data itself, and a reference (or pointer) to the next node in the sequence. Because memory allocation is dynamic, inserting or deleting nodes only requires updating pointers, making it O(1) time complexity if the location is known. However, accessing an element requires traversing the list from the head, resulting in O(N) time complexity for search operations."),
            ("What is a Stack?", "A stack is a linear data structure that follows a particular order in which operations are performed. The order is LIFO (Last In, First Out). Think of it like a stack of plates; you can only add or remove plates from the very top. The primary operations are `push` (inserting an element) and `pop` (removing the top element), both operating in O(1) time. Stacks are heavily used in computer science for parsing expressions, managing function calls (the Call Stack), and implementing 'undo' mechanisms in software."),
            ("What is a Queue?", "A queue is a linear data structure that follows the FIFO (First In, First Out) principle. Think of it like a line of people waiting for a ticket; the first person in line is the first one served. Operations involve `enqueue` (adding an element to the rear) and `dequeue` (removing an element from the front). Both operations typically run in O(1) time. Queues are essential for managing tasks in order, such as thread scheduling in operating systems, printer spooling, or handling asynchronous requests in message brokers."),
            ("What is Big O Notation?", "Big O notation is a mathematical notation used in computer science to describe the performance or complexity of an algorithm. Specifically, it describes the worst-case scenario (the upper bound) of an algorithm's growth rate as the input size (N) becomes arbitrarily large. It is used to analyze both Time Complexity (how long the algorithm takes to run) and Space Complexity (how much memory it uses). Recognizing whether an algorithm is O(1) constant, O(N) linear, or O(N^2) quadratic is fundamental to writing scalable code."),
            ("What is a Tree?", "A tree is a hierarchical data structure consisting of nodes connected by edges. Unlike arrays and linked lists, which are linear, trees are non-linear. The topmost node is called the root. Nodes with no children are called leaves. Trees are naturally recursive structures used to represent hierarchical relationships. Common examples include the file system on a computer, the DOM tree in an HTML document, or an organization chart. Specialized trees, like Binary Trees, restrict each node to having at most two children."),
            ("What is a Binary Search Tree (BST)?", "A Binary Search Tree is a specialized node-based binary tree data structure with a strict ordering property: for any given node, all nodes in its left subtree must have values strictly lesser than the node's value, and all nodes in its right subtree must have values strictly greater. This property guarantees that searching, inserting, and deleting operations can skip half of the tree at each step, operating in O(log N) time on average. However, if the tree becomes unbalanced (like a straight line), these operations degrade to O(N) time."),
            ("What is a Hash Table?", "A Hash Table is an incredibly powerful data structure used to store key-value pairs. It provides lightning-fast average time complexity of O(1) for lookups, insertions, and deletions. It achieves this by using a 'hash function', which takes a key as input, processes it mathematically, and outputs an integer index pointing to a 'bucket' in an underlying array where the value is stored. Hash tables are the underlying mechanism for dictionaries in Python and objects/Maps in JavaScript."),
            ("What is a sorting algorithm?", "A sorting algorithm is a procedure that puts elements of a list in a certain order, most commonly numerical or lexicographical order. Efficient sorting is vital for optimizing other algorithms (like binary search) that require input data to be sorted. Common simple sorting algorithms include Bubble Sort and Insertion Sort (both O(N^2) time complexity, used only for very small datasets), while modern scalable systems rely on Divide-and-Conquer algorithms like QuickSort and MergeSort, which operate at a much faster O(N log N) time complexity."),
            ("What is the difference between Time and Space Complexity?", "Time complexity evaluates how the runtime of an algorithm increases as the size of the input data increases. It counts the number of operations or steps performed. Space complexity evaluates how the memory usage of an algorithm increases relative to the input size. It accounts for memory allocated for variables, data structures, and the execution call stack. In software engineering, there is often a trade-off between the two; you can frequently make an algorithm execute faster (better time complexity) by caching data (worse space complexity).")
        ],
        "moderate": [
            ("Explain Hash Collisions and how to handle them.", "A hash collision occurs in a Hash Table when the hash function maps two distinct keys to the exact same array index or bucket. Since a bucket can only hold one primary entry, collisions must be resolved. The two standard techniques are Separate Chaining and Open Addressing. In Separate Chaining, each bucket contains a pointer to a Linked List; if a collision occurs, the new entry is simply appended to that list. In Open Addressing (e.g., linear probing), if a bucket is full, the algorithm sequentially probes for the next available empty bucket in the array to store the value."),
            ("What is recursion?", "Recursion is a programming technique where a function calls itself directly or indirectly in order to solve a problem. A recursive function must have two main components: a Base Case (a terminating condition that stops the recursion from continuing infinitely) and a Recursive Case (where the function calls itself with modified arguments, working towards the base case). While recursive code is often much cleaner and more elegant (especially for traversing trees or graphs), it consumes memory on the Call Stack for every invocation, risking a Stack Overflow if the recursion goes too deep."),
            ("What is the difference between Depth First Search (DFS) and Breadth First Search (BFS)?", "DFS and BFS are the two primary algorithms for traversing or searching tree or graph data structures. DFS dives as deep as possible down one branch before backtracking. It is typically implemented elegantly using Recursion (leveraging the system Call Stack). BFS explores the graph layer by layer, visiting all immediate neighbors before moving to the next level deep. BFS is implemented iteratively using a Queue data structure. BFS is ideal for finding the shortest path in unweighted graphs, while DFS is useful for topological sorting and solving maze puzzles."),
            ("Explain the concept of Dynamic Programming.", "Dynamic Programming (DP) is a highly optimized algorithmic paradigm that solves complex problems by breaking them down into smaller, overlapping subproblems. The core tenet is 'remembering the past'. Instead of recalculating the solution to the same subproblem multiple times (which happens in naive recursion), DP stores the results in an array or hash table. This caching technique is called Memoization (Top-Down) or Tabulation (Bottom-Up). DP drastically reduces time complexities, often converting exponential O(2^N) runtimes into polynomial O(N^2) or linear O(N) runtimes."),
            ("What is a Graph?", "A Graph is a non-linear data structure consisting of a finite set of vertices (or nodes) and a set of edges that connect pairs of vertices. Graphs are used to model pairwise relations between objects, making them the perfect data structure for representing social networks, mapping applications (GPS), network topologies, and recommendation engines. Graphs can be directed (edges point in one direction, like Twitter followers) or undirected (bidirectional, like Facebook friends), and edges can be weighted (representing distance or cost) or unweighted."),
            ("How does Merge Sort work?", "Merge Sort is a highly efficient, general-purpose sorting algorithm based on the Divide and Conquer paradigm. It works recursively. First, it continuously divides the unsorted array in half until it reaches subarrays containing only a single element (which are inherently sorted). Then, the 'Merge' phase begins: it repeatedly compares elements from the divided subarrays and merges them together into new, sorted arrays, building back up to the original size. Merge Sort guarantees an O(N log N) time complexity in all cases and is a stable sort, though it requires O(N) auxiliary space."),
            ("How does Quick Sort work?", "Quick Sort is a Divide and Conquer sorting algorithm that is generally considered the fastest in practice for in-memory sorting. It works by selecting a 'pivot' element from the array and partitioning the other elements into two sub-arrays: those less than the pivot and those greater than the pivot. The sub-arrays are then sorted recursively. Unlike Merge Sort, Quick Sort sorts in-place, requiring minimal O(log N) auxiliary space. Its average time complexity is O(N log N), though poor pivot selection (like picking the smallest element in an already sorted array) can degrade it to O(N^2)."),
            ("What is a Heap?", "A Heap is a specialized tree-based data structure that satisfies the heap property. In a Max-Heap, the parent node is always greater than or equal to its children, making the root node the absolute maximum value. In a Min-Heap, the parent is smaller, making the root the minimum. Despite being conceptualized as a binary tree, heaps are almost always implemented using a flat array to save memory. Heaps are crucial because they allow O(1) access to the max/min element and O(log N) insertion and deletion, making them the basis for Priority Queues and Heap Sort."),
            ("Explain the concept of a Trie.", "A Trie (pronounced 'try', from 'retrieval'), also known as a prefix tree, is a specialized tree-like data structure used specifically to store associative arrays where the keys are usually strings. Unlike a BST, nodes in a trie do not store the entire string; instead, a node's position in the tree defines the string it represents. The path from the root to a node spells out the prefix. Tries provide highly optimized O(L) time complexity (where L is the length of the string) for searching and inserting. They are the backbone of autocomplete features and spell checkers."),
            ("What is the Sliding Window technique?", "The Sliding Window technique is an algorithmic pattern used to perform operations on a specific window size of a given array or linked list, such as finding the longest subarray, the maximum sum of K consecutive elements, or specific string permutations. It involves defining two pointers (start and end) that bound the 'window'. Instead of running nested loops (O(N^2)), the window 'slides' through the array, adding the next element and removing the first element. This reuses previous calculations, effectively reducing complex problems to a highly efficient O(N) linear time complexity.")
        ],
        "difficult": [
            ("Explain Dijkstra’s Algorithm.", "Dijkstra's algorithm solves the single-source shortest path problem for graphs with non-negative edge weights. It finds the shortest path from a starting node to all other nodes in the graph. It maintains a set of unvisited nodes and assigns a tentative distance value to every node (infinity initially, zero for the start node). Using a Priority Queue (Min-Heap), the algorithm consistently extracts the node with the smallest distance, iterates through its neighbors, and updates their tentative distances if a shorter path is found. Its time complexity is highly optimized to O((V + E) log V) using an adjacency list and a priority queue. It is the core algorithm used in network routing protocols."),
            ("Explain A* (A-star) Search Algorithm.", "A* is an advanced graph traversal and path search algorithm, widely used in artificial intelligence and game development for pathfinding. It is an extension of Dijkstra's algorithm. While Dijkstra only considers the absolute distance traveled from the start, A* uses a heuristic function (like Euclidean or Manhattan distance) to estimate the remaining distance from the current node to the target. The algorithm evaluates nodes based on the formula f(n) = g(n) + h(n), where g is the cost from the start, and h is the estimated cost to the goal. This informed search allows A* to heavily prioritize paths moving directly toward the target, making it vastly faster than Dijkstra for point-to-point navigation."),
            ("What is a Disjoint-Set (Union-Find) data structure?", "The Disjoint-Set, or Union-Find, is a highly specialized data structure that tracks a set of elements partitioned into a number of disjoint (non-overlapping) subsets. It supports two primary, incredibly fast operations: 'Find' (determines which subset a particular element is in, allowing checks if two elements are in the same set) and 'Union' (merges two distinct subsets into a single set). By employing two crucial optimizations—Path Compression and Union by Rank—both operations operate in near-constant time, O(alpha(N)). It is heavily used in graph algorithms, most notably in Kruskal's algorithm to find the Minimum Spanning Tree and to detect cycles in an undirected graph."),
            ("Explain AVL Trees and Red-Black Trees.", "Both AVL Trees and Red-Black Trees are Self-Balancing Binary Search Trees. In a standard BST, ordered insertions can degenerate the tree into a linked list, ruining the O(log N) search time. Self-balancing trees automatically perform structural rotations (left or right) during insertions and deletions to guarantee the tree remains balanced. AVL trees enforce a strict balance factor (height difference between child subtrees is max 1), resulting in faster lookups but slower insertions due to frequent rotations. Red-Black trees use a coloring scheme (nodes are red or black) and looser balancing rules, allowing for faster insertions and deletions, making them the data structure of choice for standard library maps and sets (like C++ `std::map`)."),
            ("What is Bellman-Ford Algorithm?", "The Bellman-Ford algorithm is used to find the shortest path from a single source node to all other nodes in a weighted graph. Unlike Dijkstra's algorithm, Bellman-Ford is capable of handling graphs containing negative weight edges. It works by 'relaxing' all the edges in the graph V-1 times (where V is the number of vertices). If the distance to a destination node can be shortened by taking an edge, the distance is updated. A massive advantage of Bellman-Ford is its ability to detect negative weight cycles. If a V-th iteration produces a shorter path, a negative cycle exists. Its trade-off is performance, running in O(V * E) time."),
            ("Explain the Knapsack Problem.", "The Knapsack Problem is a classic problem in combinatorial optimization. Given a set of items, each with a weight and a value, you must determine the number of each item to include in a collection so that the total weight is less than or equal to a given limit (the knapsack capacity) and the total value is as large as possible. The 'Fractional Knapsack' allows taking parts of an item and is easily solved using a Greedy Algorithm. However, the '0/1 Knapsack' forces you to either take an entire item or leave it. This cannot be solved greedily and requires Dynamic Programming, utilizing a 2D table to store the maximum value achievable for every weight capacity up to the limit."),
            ("What are Topological Sorting and Kahn’s Algorithm?", "Topological sorting is a linear ordering of vertices in a Directed Acyclic Graph (DAG) such that for every directed edge U -> V, vertex U comes before V in the ordering. It is impossible if the graph contains a cycle. It is used heavily for scheduling tasks with dependencies, like resolving package dependencies in build systems (npm, maven) or course prerequisites. Kahn's algorithm implements this using an 'in-degree' array (counting incoming edges). It pushes all nodes with 0 in-degree into a queue. As it pops nodes off, it appends them to the result and decrements the in-degree of their neighbors, queueing neighbors when they hit 0."),
            ("What is the Rabin-Karp Algorithm?", "Rabin-Karp is a string-searching algorithm used for pattern matching. Instead of checking for a match character by character (which naive algorithms do in O(N*M) time), Rabin-Karp uses a rolling hash function. It computes a hash value for the target pattern and a hash value for every substring of the text of the same length. If the hashes match, it then performs a character-by-character check to rule out hash collisions. By using a rolling hash, the hash of the next substring is computed in O(1) time by mathematically dropping the first character and adding the next. Its average time complexity is a highly efficient O(N+M)."),
            ("Explain segment trees.", "A Segment Tree is a highly advanced data structure used for storing information about intervals or segments. It allows answering range queries (like finding the sum, minimum, or maximum value within an array range from index L to index R) and updating array elements both in highly efficient O(log N) time. Unlike a prefix sum array, which answers range sums in O(1) but takes O(N) to update an element, a segment tree balances both operations. It is constructed as a binary tree where the leaves represent the array elements, and internal nodes represent the merged result of their children intervals. It is heavily utilized in competitive programming and spatial data processing."),
            ("What is the Floyd-Warshall Algorithm?", "The Floyd-Warshall algorithm is a Dynamic Programming algorithm used to find the shortest paths between all pairs of vertices in a weighted graph. Unlike Dijkstra or Bellman-Ford, which find paths from a single source, Floyd-Warshall computes the optimal paths between every possible pair of nodes simultaneously. It works iteratively, maintaining a 2D matrix of minimum distances. In each phase, it considers whether routing a path through a specific intermediate vertex 'K' provides a shorter route than the previously known direct route. Despite being conceptually simple to implement with three nested loops, its execution time complexity is extremely heavy at O(V^3), making it suitable only for relatively small, dense graphs.")
        ]
    }
}


def build_final_json():
    final_output = []
    
    for skill_meta in skills_metadata:
        skill_id = skill_meta["id"]
        
        skill_obj = {
            "id": skill_id,
            "name": skill_meta["name"],
            "icon": "",
            "questions": []
        }
        
        deep_dive = DEEP_DIVES.get(skill_id, "")
        
        for difficulty in ["easy", "moderate", "difficult"]:
            questions = questions_data.get(skill_id, {}).get(difficulty, [])
            for idx, (q_text, q_ans) in enumerate(questions):
                
                # Combine specific answer with deep dive to ensure 500-1000 word length
                combined_answer = f"{q_ans}\\n\\n{deep_dive}"
                
                # Generate unique ID
                q_id = f"{skill_id}-{difficulty}-{idx + 1}"
                
                # Alternate highly asked
                is_highly = (idx % 2 == 0)
                
                skill_obj["questions"].append({
                    "id": q_id,
                    "question": q_text,
                    "answer": combined_answer,
                    "difficulty": difficulty,
                    "isHighlyAsked": is_highly
                })
        
        final_output.append(skill_obj)
        
    return final_output

if __name__ == "__main__":
    out_path = os.path.join(os.path.dirname(__file__), "..", "FRONTEND", "src", "components", "Interview", "realInterviewData.json")
    
    with open(out_path, "w", encoding="utf-8") as f:
        json.dump(build_final_json(), f, indent=2, ensure_ascii=False)
        
    print(f"Successfully generated {len(skills_metadata) * 30} questions and saved to {os.path.abspath(out_path)}")
